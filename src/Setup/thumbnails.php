<?php

namespace {{ theme.namespace }}\Setup;

/**
 * Register custom image sizes
 * for posts thumbnails.
 *
 * @return void
 */
add_action('after_setup_theme', function () {
    // add_image_size('custom-thumbnail', 800, 600, true);
});
